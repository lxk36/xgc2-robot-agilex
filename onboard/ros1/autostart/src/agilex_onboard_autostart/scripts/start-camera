#!/usr/bin/env bash
set -euo pipefail

ROS_DISTRO="${ROS_DISTRO:-melodic}"
ROS_PREFIX="/opt/ros/${ROS_DISTRO}"
# shellcheck disable=SC1091
source "${ROS_PREFIX}/setup.bash"

export ROS_MASTER_URI="${ROS_MASTER_URI:-http://127.0.0.1:11311}"
export ROS_IP="${ROS_IP:-127.0.0.1}"

HERE="$(cd "$(dirname "$0")" && pwd)"
"${HERE}/wait-roscore"
stagger="${CAMERA_STAGGER_SEC:-6}"
echo "xgc2 agilex camera: stagger ${stagger}s after roscore"
sleep "${stagger}"

sec="${CAMERA_SETTLE_SEC:-5}"
echo "xgc2 agilex camera: settle ${sec}s"
sleep "${sec}"

exec roslaunch --wait agilex_onboard_autostart camera-media.launch
